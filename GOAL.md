# Phase 1 pi-goal: Does precision leave state?

Paste the following as one command into Pi from the repository root.

```text
/goal --tokens 80k Determine whether weight precision is a stateful part of autoregressive inference on the available Qwen3-4B nested-quantized setup, and finish with an evidence-backed continue, revise, stop, or pause decision. The completed outcome must include: (1) a tested harness that changes W4/W8 precision between cached autoregressive forwards while recording the actual applied schedule; (2) a controlled teacher-forced study in which every comparison uses identical token IDs, equal counts of W4 and W8 history tokens, the same W8 precision at the probe token, and a genuinely reused KV cache; (3) a separate deterministic free-running study of fixed W4/W6/W8 and two-segment W4→W8/W8→W4 schedules that records first token divergence, EOS, output length, length-cap hits, and actual KV bytes; and (4) PRECISION_STATE_REPORT.md, PRECISION_STATE_AUDIT.md, and DECISION.md that separate confirmed findings, unknowns, and proposed next work.

Use this repository for all new code and outputs. Treat weige15/qaq_baseline at exact commit a5f4d5d52358c7b9740ecec448db5279c6af5ddc as read-only input: verify the commit and hash every imported source/checkpoint, never edit or repair that repository, and state that its nested weights reconstruct FP16 computation rather than providing a low-bit kernel. First inspect the local environment and establish whether the upstream checkpoint/evidence bundle is accessible. If it is absent, do not download a large model or regenerate closed evidence without explicit permission: complete all possible tiny-model/CPU correctness work, then pause with the exact missing path or artifact.

Implement controls before hypothesis measurements. Tests must prove schedule validation, equal W4/W8 history counts, identical teacher-forced tokens, cache reuse across chunks, append-only result paths, exact KV-byte accounting, deterministic selection, and decision boundaries. On a tiny causal model, verify that constant W8 and constant W4 chunked-cache logits match their corresponding one-shot logits within a justified numerical tolerance. Repeat that control on the real model in the pilot. Any unexplained constant-precision chunking difference large enough to overlap the proposed history signal blocks interpretation.

Use a non-decision pilot of no more than four prompts only to verify cache semantics, choose viable deterministic prompt formatting from already available inputs, estimate repeat/chunking noise, and project runtime. Do not use pilot mixed-history comparisons to choose thresholds or schedules. Before any main cross-schedule result is inspected, freeze PRECISION_STATE_PROTOCOL.md and configs/precision_state_v1.json with exact prompt IDs and hashes, pilot/main separation, prefix and segment lengths, W4/W8 schedules, W8 probe definition, decoding settings, seeds, metrics, practical thresholds, uncertainty calculation, exclusions, and decision rules. Justify the numerical noise gate from repeated identical controls and define a separate practical-consequence gate; neither may be changed after main results. Target at least 32 main prompts and never make a scientific continue/stop claim from fewer than 16. If the projected safe cost cannot support 16, pause rather than silently shrinking the study.

For the primary teacher-forced comparison, build one fixed token prefix in two equal-length cached chunks under W8→W4 versus W4→W8, then evaluate the same probe token under W8. Include constant W8→W8 and W4→W4 controls and fresh-cache repeats. Save full probe logits or a lossless equivalent in ignored append-only raw artifacts, plus tracked hashes and independently recomputable summaries including max absolute logit difference, a probability-distribution distance, top-1 changes, top-k overlap, per-layer KV shapes/dtypes/bytes, and schedule traces. The report may call this a precision-history effect only if it exceeds both frozen gates, survives both fresh processes, and the constant-precision controls pass.

For free-running generation, use the same frozen prompts and deterministic decoding, keep fixed W4/W6/W8 separate from equal-segment W4→W8 and W8→W4 schedules, and record complete token IDs. Report paired per-prompt changes and their uncertainty for EOS, output length, first divergence, and peak/cumulative KV bytes. If EOS occurs before equal schedule exposure or a run reaches the length cap, retain and label it; do not claim equal realized bit budgets. Do not attribute free-running differences specifically to KV history because token paths can diverge.

Before each GPU job, use one idle process-free GPU and a recorded preflight, never kill or displace another process, enforce a 30-minute timeout per job, run jobs serially, and cap all work under this goal at four GPU-hours including failed attempts. Do not install or upgrade packages. Preserve raw outputs under new append-only run directories, record commands, environment, model/tokenizer revisions, GPU identity, wall time, and SHA256 manifests, and run two fresh main processes only after the frozen protocol authorizes them.

After execution, have an analysis path and a separate audit path independently recompute all primary comparisons and the final branch. Run focused tests, all project CPU tests, artifact/hash checks, repeat-agreement checks, and git diff --check. The final decision must be exactly one of: continue_precision_provenance if the controlled history effect passes; revise_endogenous_workload if only the free-running practical gate passes; continue_joint_state_work if both pass; stop_precision_state_direction if neither passes with valid evidence; or pause_blocked if validity/resources prevent the frozen minimum evidence. Do not build a scheduler, continuous batching engine, KV quantizer, router, custom kernel, async loader, or multi-GPU system under this goal, and do not claim latency, throughput, memory, quality, or novelty beyond what was directly measured. Between iterations, inspect the strongest unmet completion criterion, run the smallest test that can falsify the current assumption, and re-audit scope and preserved evidence. If blocked or no valid path remains, stop with the evidence collected, attempts made, exact blocker, and next user input required rather than substituting a weaker experiment.
```

## Completion contract map

| pi-goal requirement | Where it appears |
|---|---|
| Outcome | Harness, two studies, three final documents, one explicit decision |
| Verification | Tiny and real-model controls, frozen protocol, two fresh processes, independent audit |
| Constraints | Read-only upstream, no overclaiming, no package changes, GPU limits |
| Boundaries | No scheduler, KV quantizer, router, kernel, loader, or multi-GPU work |
| Iteration policy | Test the strongest unmet criterion with the smallest falsifying test |
| Blocked stop | `pause_blocked` with evidence, attempted paths, blocker, and needed input |
